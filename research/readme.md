# research

fix favicon?

## done

~~~
appConfigDir := filepath.Join(configDir, "chatbot")
~~~

struct pointers

~~~
configData, _ := json.MarshalIndent(cfg, "", "  ")
~~~

split into package

~~~
if inItalic { out.WriteString("</em>") }
~~~
